# ConfigMap mit dem Datenbankpfad fuer die SQLite Persistenz auf der PVC.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "price-watch.fullname" . }}
  labels:
    {{- include "price-watch.labels" . | nindent 4 }}
data:
  DATABASE_PATH: /data/prices.db